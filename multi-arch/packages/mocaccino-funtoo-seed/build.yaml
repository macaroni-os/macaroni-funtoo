requires:
- category: "layer"
  name: "funtoo-stage3"
  version: ">=0"
steps:
# I maintain a more thin image to speedup download.
- rm -rf ./lib64/modules/*
# Setup funtoo profile and kits
#- ego sync && epro list
# TODO: add mocaccino-overlay
