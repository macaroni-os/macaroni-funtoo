install:
- source /etc/profile && env
- source /var/lib/mocaccino/live-functions.sh && prepare
- ln -s /boot/kernel-* /boot/bzImage || true
- ln -s /boot/initramfs-* /boot/Initrd || true
shell:
- bash
- -c
