name: docker-model
version: 1.2.3
category: app-ai
use_flags:
- compress
annotations:
  subsets:
    rules:
      devel:
      - ^/usr/include/
      portage:
      - ^/var/db/pkg/
description: Docker Model Runner
uri:
- https://github.com/docker/model-runner
license: Apache-2.0
labels:
  BDEPEND: virtual/pkgconfig compress? ( app-arch/upx-bin ) >=dev-lang/go-1.12 app-arch/unzip
  IUSE: compress +runner
  emerge.packages: app-ai/docker-model
  kit: ai-kit
  original.package.name: app-ai/docker-model
  original.package.slot: "0"
  original.package.version: 1.2.3
