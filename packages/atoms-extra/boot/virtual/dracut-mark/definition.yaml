name: dracut-mark
version: "1"
category: virtual
annotations:
  subsets:
    rules:
      devel:
      - ^/usr/include/
      portage:
      - ^/var/db/pkg/
description: A virtual package that installs a Dracut config for MARK
labels:
  emerge.packages: virtual/dracut-mark
  kit: kernel-kit
  original.package.name: virtual/dracut-mark
  original.package.slot: "0"
  original.package.version: "1"
