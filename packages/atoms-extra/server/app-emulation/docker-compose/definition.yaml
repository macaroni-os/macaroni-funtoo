name: docker-compose
version: 5.1.3
category: app-emulation
requires:
- name: docker-cli
  version: '>=0'
  category: app-emulation
annotations:
  subsets:
    rules:
      devel:
      - ^/usr/include/
      portage:
      - ^/var/db/pkg/
description: Define and run multi-container applications with Docker
uri:
- https://docs.docker.com/compose/
license: Apache-2.0
labels:
  BDEPEND: '>=dev-lang/go-1.12 app-arch/unzip'
  RDEPEND: app-emulation/docker-cli
  emerge.packages: app-emulation/docker-compose
  kit: core-server-kit
  original.package.name: app-emulation/docker-compose
  original.package.slot: "0"
  original.package.version: 5.1.3
