name: kubectl
version: 1.30.0
category: sys-cluster
annotations:
  subsets:
    rules:
      devel:
      - ^/usr/include/
      portage:
      - ^/var/db/pkg/
description: CLI to run commands against Kubernetes clusters
uri:
- https://kubernetes.io
license: Apache-2.0
labels:
  BDEPEND: '>=dev-lang/go-1.21 >=dev-lang/go-1.12 app-arch/unzip'
  DEPEND: '!sys-cluster/kubernetes'
  IUSE: hardened
  emerge.packages: sys-cluster/kubectl
  kit: core-kit
  original.package.name: sys-cluster/kubectl
  original.package.slot: "0"
  original.package.version: 1.30.0
