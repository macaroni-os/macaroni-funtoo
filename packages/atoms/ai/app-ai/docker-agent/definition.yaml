name: docker-agent
version: 1.84.0
category: app-ai
use_flags:
- compress
annotations:
  subsets:
    rules:
      devel:
      - ^/usr/include/
      portage:
      - ^/var/db/pkg/
description: AI Agent Builder and Runtime by Docker Engineering
uri:
- https://docker.github.io/docker-agent/
license: Apache-2.0
labels:
  BDEPEND: virtual/pkgconfig compress? ( app-arch/upx-bin ) >=dev-lang/go-1.12 app-arch/unzip
  IUSE: +compress
  emerge.packages: app-ai/docker-agent
  kit: ai-kit
  original.package.name: app-ai/docker-agent
  original.package.slot: "0"
  original.package.version: 1.84.0
