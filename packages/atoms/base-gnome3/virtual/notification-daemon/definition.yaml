name: notification-daemon
version: 1+2
category: virtual
use_flags:
- -kde
- gnome
- elogind
- X
- wayland
- xwayland
- udev
- networkmanager
annotations:
  subsets:
    rules:
      devel:
      - ^/usr/include/
      portage:
      - ^/var/db/pkg/
description: Virtual for notification daemon dbus service
labels:
  IUSE: gnome kde
  RDEPEND: gnome? ( || ( x11-misc/notification-daemon gnome-base/gnome-shell ) ) kde?
    ( kde-plasma/plasma-workspace ) !gnome? ( !kde? ( || ( x11-misc/notification-daemon
    xfce-extra/xfce4-notifyd x11-wm/enlightenment[enlightenment_modules_notification]
    x11-wm/enlightenment[e_modules_notification] lxqt-base/lxqt-notificationd ) )
    )
  emerge.packages: virtual/notification-daemon
  kit: gnome-kit
  original.package.name: virtual/notification-daemon
  original.package.slot: "0"
  original.package.version: "1"
