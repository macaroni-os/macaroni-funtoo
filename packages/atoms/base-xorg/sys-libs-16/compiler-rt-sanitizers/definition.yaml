name: compiler-rt-sanitizers
version: 16.0.6+2
category: sys-libs-16
use_flags:
- clang
- elibc_glibc
annotations:
  subsets:
    rules:
      devel:
      - ^/usr/include/
      portage:
      - ^/var/db/pkg/
description: Compiler runtime libraries for clang (sanitizers & xray)
uri:
- https://llvm.org/
license: Apache-2.0-with-LLVM-exceptions || ( UoI-NCSA MIT )
labels:
  BDEPEND: dev-util/cmake clang? ( sys-devel/clang ) elibc_glibc? ( net-libs/libtirpc
    ) || ( dev-lang/python:3.8 dev-lang/python:3.9 dev-lang/python:3.7 dev-lang/python:3.10
    ) dev-util/ninja dev-util/cmake
  DEPEND: sys-devel/llvm
  IUSE: +clang debug elibc_glibc +libfuzzer +memprof +orc +profile +xray +asan +dfsan
    +lsan +msan +hwasan +tsan +ubsan +safestack +cfi +scudo +shadowcallstack +gwp-asan
  emerge.packages: sys-libs/compiler-rt-sanitizers:16
  kit: llvm-kit
  original.package.name: sys-libs/compiler-rt-sanitizers
  original.package.slot: "16"
  original.package.version: 16.0.6-r1
