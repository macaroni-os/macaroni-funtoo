package_dir: /pkgdir/{{ .Values.name | replace "-portage" "" }}
includes:
  - ^/var/db/pkg
  - ^/var/db$
  - ^/var$
requires:
  - name: sqlite
    version: '{{ .Values.version }}'
    category: dev-db-3
