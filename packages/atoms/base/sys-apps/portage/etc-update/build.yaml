prelude:
- >-
  eval 'rm -rf /pkgdir/ || true' &&
  mkdir -p /pkgdir/{{ .Values.name }}/etc &&
  mkdir -p /pkgdir/{{ .Values.name }}/usr/sbin/ &&
  cp -a /usr/sbin/etc-update /pkgdir/{{ .Values.name }}/usr/sbin/ &&
  cp -a /etc/etc-update.conf /pkgdir/{{ .Values.name }}/etc/
package_dir: /pkgdir/{{ .Values.name }}
requires:
- name: portage
  version: '>=0'
  category: sys-apps
