name: dev-manager
version: 0+2
category: virtual
requires:
  - name: udev
    version: '>=0'
    category: virtual
description: Virtual for the device filesystem manager
uri:
  - ""
labels:
  RDEPEND: '|| ( virtual/udev sys-apps/busybox[mdev] sys-fs/static-dev sys-freebsd/freebsd-sbin )'
  emerge.packages: virtual/dev-manager
  kit: core-kit
  original.package.name: virtual/dev-manager
  original.package.version: 0-r2
annotations:
  subsets:
    rules:
      devel:
        - ^/usr/include/
      portage:
        - ^/var/db/pkg/
