name: pam
version: 0+1
category: virtual
requires:
- name: pam
  version: '>=0'
  category: sys-libs
annotations:
  subsets:
    rules:
      devel:
      - ^/usr/include/
      portage:
      - ^/var/db/pkg/
description: Virtual for PAM (Pluggable Authentication Modules)
uri:
- ""
labels:
  RDEPEND: sys-libs/pam
  emerge.packages: virtual/pam
  kit: core-kit
  original.package.name: virtual/pam
  original.package.slot: "0"
  original.package.version: 0-r1
