package_dir: /pkgdir/{{ .Values.name | replace "-portage" "" }}
includes:
  - ^/var/db/pkg
  - ^/var/db$
  - ^/var$
requires:
  - name: perl-Compress-Raw-Zlib
    version: '{{ .Values.version }}'
    category: virtual
