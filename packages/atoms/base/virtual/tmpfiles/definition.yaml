name: tmpfiles
version: "3"
category: virtual
requires:
- name: systemd
  version: '>=0'
  category: sys-apps
annotations:
  subsets:
    rules:
      devel:
      - ^/usr/include/
      portage:
      - ^/var/db/pkg/
description: Virtual to select between different tmpfiles.d handlers
uri:
- ""
labels:
  IUSE: systemd
  RDEPEND: '!systemd? ( sys-apps/systemd-tmpfiles ) systemd? ( sys-apps/systemd )'
  emerge.packages: virtual/tmpfiles
  kit: core-kit
  original.package.name: virtual/tmpfiles
  original.package.slot: "0"
  original.package.version: "3"
