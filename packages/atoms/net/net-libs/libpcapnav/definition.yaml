name: libpcapnav
version: "0.8"
category: net-libs
requires:
- name: libpcap
  version: '>=0'
  category: net-libs
annotations:
  subsets:
    rules:
      devel:
      - ^/usr/include/
      portage:
      - ^/var/db/pkg/
description: Libpcap wrapper library to navigate to arbitrary packets in a tcpdump
  trace file
uri:
- http://netdude.sourceforge.net/
license: BSD
labels:
  DEPEND: net-libs/libpcap
  IUSE: doc static-libs
  RDEPEND: net-libs/libpcap
  emerge.packages: net-libs/libpcapnav
  kit: net-kit
  original.package.name: net-libs/libpcapnav
  original.package.slot: "0"
  original.package.version: "0.8"
