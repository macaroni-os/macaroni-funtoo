name: psmisc
version: "23.2"
category: sys-process
use_flags:
- -nls
- -selinux
requires:
- name: ncurses
  version: '>=0'
  category: sys-libs
annotations:
  subsets:
    rules:
      devel:
      - ^/usr/include/
      portage:
      - ^/var/db/pkg/
description: A set of tools that use the proc filesystem
uri:
- http://psmisc.sourceforge.net/
license: GPL-2
labels:
  DEPEND: '>=sys-libs/ncurses-5.7-r7:0= nls? ( virtual/libintl ) selinux? ( sys-libs/libselinux
    ) >=sys-devel/libtool-2.2.6b nls? ( sys-devel/gettext )'
  IUSE: ipv6 nls selinux X
  RDEPEND: '>=sys-libs/ncurses-5.7-r7:0= nls? ( virtual/libintl ) selinux? ( sys-libs/libselinux
    )'
  emerge.packages: sys-process/psmisc
  kit: core-kit
  original.package.name: sys-process/psmisc
  original.package.slot: "0"
  original.package.version: 23.2-r2
