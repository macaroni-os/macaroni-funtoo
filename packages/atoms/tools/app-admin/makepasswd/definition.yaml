name: makepasswd
version: "1.10"
category: app-admin
requires:
- name: perl
  version: '>=0'
  category: dev-lang
annotations:
  subsets:
    rules:
      devel:
      - ^/usr/include/
      portage:
      - ^/var/db/pkg/
description: Random password generator
uri:
- https://packages.debian.org/stable/admin/makepasswd
license: GPL-2
labels:
  RDEPEND: dev-lang/perl
  emerge.packages: app-admin/makepasswd
  kit: geaaru-kit
  original.package.name: app-admin/makepasswd
  original.package.slot: "0"
  original.package.version: "1.10"
