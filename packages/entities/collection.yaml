# This collections contains temporary packages used
# by entities merge command that will be replaced
# when i have integrated luet-portage-converter
# when an automatic management of the acct/* packages.

packages:
  - name: acct-base
    version: "0.20210930"
    category: entities
    desciption: |
      Base system entities.
    labels:
      entities_dir: "funtoo-base"
    requires:
      - name: entities
        category: system
        version: ">=0"
      - name: sh
        category: "virtual"
        version: ">=0"
      - name: funtoo-base
        category: "pkglist"
        version: ">=0"

  - name: acct-boot
    version: "0.20210930"
    category: entities
    desciption: |
      Boot system entities.
    labels:
      entities_dir: "funtoo-boot"
    requires:
      - name: entities
        category: system
        version: ">=0"
      - name: sh
        category: "virtual"
        version: ">=0"
      - name: funtoo-boot
        category: "pkglist"
        version: ">=0"

  - name: acct-emulation
    version: "0.20210930"
    category: entities
    desciption: |
      Emulation system entities.
    labels:
      entities_dir: "funtoo-emulation"
    requires:
      - name: entities
        category: system
        version: ">=0"
      - name: sh
        category: "virtual"
        version: ">=0"
      - name: funtoo-emulation
        category: "pkglist"
        version: ">=0"

  - name: acct-server
    version: "0.20210930"
    category: entities
    desciption: |
      Server system entities.
    labels:
      entities_dir: "funtoo-server"
    requires:
      - name: entities
        category: system
        version: ">=0"
      - name: sh
        category: "virtual"
        version: ">=0"
      - name: funtoo-server
        category: "pkglist"
        version: ">=0"

  - name: acct-office
    version: "0.20210930"
    category: entities
    desciption: |
      Office system entities.
    labels:
      entities_dir: "funtoo-office"
    requires:
      - name: entities
        category: system
        version: ">=0"
      - name: sh
        category: "virtual"
        version: ">=0"
      - name: funtoo-office
        category: "pkglist"
        version: ">=0"

  - name: acct-media-gfx
    version: "0.20210930"
    category: entities
    desciption: |
      Media GFX system entities.
    labels:
      entities_dir: "funtoo-media-gfx"
    requires:
      - name: entities
        category: system
        version: ">=0"
      - name: sh
        category: "virtual"
        version: ">=0"
      - name: funtoo-media-gfx
        category: "pkglist"
        version: ">=0"
