category: "kernel-5.10"
name: "virtualbox-guest-additions"
version: "6.1.26"
provides:
  - category: app-emulation
    name: virtualbox-guest-additions
    # BUG: setting a version on provides is not
    # handled correctly by validate.
    version: ">=0"
labels:
  kernel.version: "5.10.61"
  kernel.suffix: "macaroni"
  emerge.packages: >-
    app-emulation/virtualbox-guest-additions
annotations:
  subsets:
    devel:
      - "^/usr/include/"
    portage:
      - "^/var/db/pkg/"

