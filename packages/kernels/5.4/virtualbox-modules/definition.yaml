category: "kernel-5.4"
name: "virtualbox-modules"
version: "6.1.26+1"
provides:
  - category: app-emulation
    name: virtualbox-modules
    # BUG: setting a version on provides is not
    # handled correctly by validate.
    version: ">=0"
labels:
  emerge.packages: >-
    app-emulation/virtualbox-modules
  kernel.version: "5.4.165"
  kernel.suffix: "macaroni"
