extra-pkgs:
  kits:
    browser-kit:
      specs:
        - browsers-extra.yaml

    dev-kit:
      specs:
        - dev-extra.yaml
        - macaroni-emulation-extra.yaml
        - php-extra.yaml

    core-hw-kit:
      specs:
        - dev-extra.yaml

    core-kit:
      specs:
        - dev-extra.yaml
        - macaroni-emulation-extra.yaml
        - net-extra.yaml
        - php-extra.yaml

    core-server-kit:
      specs:
        - dev-extra.yaml
        - php-extra.yaml
        - python-extra.yaml

    desktop-kit:
      specs:
        - dev-extra.yaml
        - desktop-extra.yaml

    java-kit:
      specs:
        - dev-extra.yaml

    kde-kit:
      specs:
        - dev-extra.yaml
        - media-extra.yaml

    media-kit:
      specs:
        - dev-extra.yaml
        - media-extra.yaml

    net-kit:
      specs:
        - dev-extra.yaml
        - macaroni-emulation-extra.yaml
        - net-extra.yaml
        - php-extra.yaml

    python-modules-kit:
      specs:
        - dev-extra.yaml
        - macaroni-emulation-extra.yaml
        - python-extra.yaml

    text-kit:
      specs:
        - dev-extra.yaml
        - php-extra.yaml

    xfce-kit:
      specs:
        - dev-extra.yaml

    gnome-kit:
      specs:
        - mos-gnome-extra.yaml

    qt-kit:
      specs:
        - net-extra.yaml

    nokit:
      specs:
        - net-extra.yaml
