funtoo-stage:

  kits:
    browser-kit:
      specs:
        - base-custom.yaml
        - base.yaml

    core-hw-kit:
      specs:
        - base-custom.yaml
        - base.yaml

    core-kit:
      specs:
        - base-custom.yaml
        - base.yaml

    core-server-kit:
      specs:
        - base-custom.yaml
        - base.yaml

    desktop-kit:
      specs:
        - base-custom.yaml
        - base.yaml

    dev-kit:
      specs:
        - base-custom.yaml
        - base.yaml

    editors-kit:
      specs:
        - base-custom.yaml
        - base.yaml

    gnome-kit:
      specs:
        - base-custom.yaml
        - base.yaml

    lang-kit:
      specs:
        - base-custom.yaml
        - base.yaml

    nokit:
      specs:
        - base-custom.yaml
        - base.yaml

    perl-kit:
      specs:
        - base-custom.yaml
        - base.yaml

    python-kit:
      specs:
        - base-custom.yaml
        - base.yaml

    python-modules-kit:
      specs:
        - base-custom.yaml
        - base.yaml

    security-kit:
      specs:
        - base-custom.yaml
        - base.yaml

    text-kit:
      specs:
        - base-custom.yaml
        - base.yaml

# grep "kit:" packages/atoms-extra/java/* -r | awk '{ print $3 }' | sort | uniq
extra-pkgs:
  kits:

    browser-kit:
      specs:

    dev-kit:
      specs:
        - base-extra.yaml
        - base-tools.yaml
        - base-tools2.yaml

    core-gl-kit:
      specs:

    core-hw-kit:
      specs:
        - base-extra.yaml

    core-kit:
      specs:
        - release.yaml
        - base-extra.yaml
        - base-python.yaml
        - base-tools.yaml
        - base-tools2.yaml

    core-server-kit:
      specs:
        - base-tools2.yaml

    lang-kit:
      specs:
        - base-python.yaml
        - base-tools.yaml

    lisp-scheme-kit:
      specs:
        - base-tools2.yaml

    media-kit:
      specs:
        - xorg-extra.yaml

    security-kit:
      specs:
        - base-extra.yaml

    text-kit:
      specs:
        - base-tools.yaml

    gnome-kit:
      specs:
        - base-tools.yaml
        - base-tools2.yaml

    net-kit:
      specs:
        - base-tools.yaml
        - base-tools2.yaml

    python-modules-kit:
      specs:
        - base-python.yaml

    perl-kit:
      specs:
        - base-extra.yaml
        - base-tools.yaml

    science-kit:
      specs:

    editors-kit:
      specs:

    geaaru-kit:
      specs:

    gnome-kit:
      specs:

    desktop-kit:
      specs:

    java-kit:
      specs:

    llvm-kit:
      specs:

seeds:
  - name: macaroni-funtoo-base
    prefix: packages/atoms
    specs:
      - file: release.yaml
        dir: release

      - file: base-extra.yaml
        dir: base-extra

  - name: macaroni-funtoo-race1
    prefix: packages/atoms
    specs:
      - file: base-python.yaml
        dir: base-python

  - name: macaroni-funtoo-race2
    prefix: packages/atoms
    specs:
      - file: base-tools.yaml
        dir: base-tools

  - name: macaroni-funtoo-race3
    prefix: packages/atoms
    specs:
      - file: base-tools2.yaml
        dir: base-tools2

  - name: macaroni-funtoo-race4
    prefix: packages/atoms
    specs:
      - file: net.yaml
        dir: net

  - name: macaroni-funtoo-race5
    prefix: packages/atoms
    specs:
      - file: lang.yaml
        dir: lang

  - name: macaroni-funtoo-race6
    prefix: packages/atoms
    specs:
      - file: dev-tools.yaml
        dir: dev-tools

  - name: macaroni-funtoo-race7
    prefix: packages/atoms
    specs:

      - file: server.yaml
        dir: server
      - file: text.yaml
        dir: text

  - name: macaroni-funtoo-race8
    prefix: packages/atoms
    specs:

      - file: xorg-base.yaml
        dir: xorg-base

  - name: macaroni-funtoo-race9
    prefix: packages/atoms
    specs:

      - file: xorg-base2.yaml
        dir: xorg-base2

  - name: macaroni-funtoo-race10
    prefix: packages/atoms
    specs:

      - file: xorg-base3.yaml
        dir: xorg-base3

      - file: latex.yaml
        dir: latex

      - file: telemetry.yaml
        dir: telemetry

      - file: base-php.yaml
        dir: base-php

  - name: macaroni-funtoo-race11
    prefix: packages/atoms
    specs:

      - file: db.yaml
        dir: db

      - file: latex-extra.yaml
        dir: latex-extra

      - file: java.yaml
        dir: java

      - file: php-extra.yaml
        dir: php-extra

  - name: macaroni-funtoo-race12
    prefix: packages/atoms
    specs:

      - file: tools.yaml
        dir: tools

      - file: python-extra.yaml
        dir: python-extra
